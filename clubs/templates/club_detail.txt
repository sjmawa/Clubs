{% extends 'base.html' %}
{% load static %}

{% block content %}
<div class="relative flex justify-center mt-5 mb-5">
    {% if club.image %}
        <img src="{{ club.image.url }}" alt="{{ club.name }}"
             class="w-2/3 h-[420px] object-cover rounded-2xl shadow-md border-4 border-gray-300" />
    {% else %}
        <img src="{% static 'images/default_club.png' %}" alt="{{ club.name }}"
             class="w-2/3 h-[420px] object-cover rounded-2xl shadow-md border-4 border-gray-300" />
    {% endif %}
    <div class="absolute top-1/2 left-0 w-full h-1 bg-gray-300 -z-10"></div>
</div>
{% endblock %}